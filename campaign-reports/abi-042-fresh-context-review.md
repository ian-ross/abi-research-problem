# ABI-042 Fresh-Context Review

## Review

- **Blocker: None.** No blocking finding exists. The diagnosis, package-manager repair, and smoke evidence address ABI-042 without changing scientific policy or launching training.

- **Correct — Root cause is evidence-backed.** The preserved pre-repair runner failure reports `MODULE_NOT_FOUND` for `typebox/compile`, originating from `pi-subagents/src/runs/shared/structured-output.ts` (`/tmp/pi-subagents-uid-247397/async-subagent-runs/cf76106e-e28d-4da1-8d02-a85ebb0d13e9/runner.stderr.log:1-4`). The repair report accurately attributes the affected installation to `pi-subagents@0.35.1` and its optional TypeBox peer (`campaign-reports/abi-042-pi-subagents-runtime-repair.md:25-35`). The installed upstream changelog confirms that 0.35.0 moved TypeBox to an optional peer and 0.36.0 restored it as a production dependency for detached runners (`/home/iross/.pi/agent/npm/node_modules/pi-subagents/CHANGELOG.md:413,437-446`).

- **Correct — The repair is durable and package-manager-owned.** The managed root manifest now declares `pi-subagents: "^0.48.0"` (`/home/iross/.pi/agent/npm/package.json:1-7`), while the installed package is version `0.48.0` and declares exact production dependency `typebox: "1.1.38"` (`/home/iross/.pi/agent/npm/node_modules/pi-subagents/package.json:2,72-76`). The lockfile records both packages and their registry integrity metadata (`/home/iross/.pi/agent/npm/package-lock.json:6-58`). TypeBox exports `./compile` to `./build/compile/index.mjs` (`/home/iross/.pi/agent/npm/node_modules/typebox/package.json:15-27`). This supports the report’s claim that the repair does not rely on `NODE_PATH`, symlinks, or a patched package file (`campaign-reports/abi-042-pi-subagents-runtime-repair.md:37-51`).

- **Correct — Structured-output validation passed.** Run `feae7b2d-5175-4229-9029-2c0def79d988` is recorded as `complete`, with a structured step and exit code 0 (`/tmp/pi-subagents-uid-247397/async-subagent-runs/feae7b2d-5175-4229-9029-2c0def79d988/status.json:7,36-44,83-84`). Its schema-bound result reports package `0.48.0`, production TypeBox dependency `true`, and marker `ABI_042_STRUCTURED_OUTPUT_OK` (`…/structured-output/pi-subagent-structured-EDllEm/output.json:1`; `/tmp/abi-042-structured-smoke.md:1-5`).

- **Correct — Ordinary fresh-context delegation works.** This fresh-context reviewer launched successfully, performed the requested read-only inspection, and returns this configured persisted artifact. That supplies the ordinary non-structured path and fresh-context review evidence required by ABI-042 acceptance criteria 3 and 4 (`backlog/tasks/abi-042 - Restore-pi-subagents-TypeBox-runtime-dependency.md:21-28`).

- **Correct — Scope respects scientific guardrails.** Repository status reported only the untracked ABI-042 repair report and one unrelated pre-existing ABI-039 temporary path; tracked `git diff --stat` was empty. Thus there is no tracked scientific-policy, candidate, provider, or training-configuration change. This agrees with the repair’s explicit scope (`campaign-reports/abi-042-pi-subagents-runtime-repair.md:3-5`) and the prohibition on local real training (`AGENTS.md:10-18`). No Candidate Run was launched as part of the reviewed repair.

- **Note — Completion bookkeeping remains pending (low severity).** `backlog task ABI-042 --plain` remains **In Progress**, with all five acceptance criteria unchecked and no final summary. The repair report’s validation section is still prospective and delegates smoke-command/artifact details to a future final task summary (`campaign-reports/abi-042-pi-subagents-runtime-repair.md:53-61`). The technical evidence now supports the criteria, but the task should only be marked Done after the CLI checklist, validation notes, and final summary are updated per `AGENTS.md:54-68`. This is normal completion bookkeeping, not a defect in the repair.